# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.WidgetController.index

# Widgets
GET     /widgets                    controllers.WidgetController.Display
GET     /javascriptRoutes           controllers.WidgetController.javascriptRoutes
GET     /autocomplete               controllers.WidgetController.fetch
POST    /widgets                    controllers.WidgetController.Update

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
